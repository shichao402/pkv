<!-- 本文件由 `dec pull` 从 .dec/cache/cli/ 渲染生成，请勿直接编辑。
     修改流程：编辑 .dec/cache/cli/... → dec push → dec pull 验证 -->

---
description: CLI 项目版本号管理与版本到构建契约规则（通用，无语言绑定，无 CI/CD 平台耦合）
globs: version.json, package.json, pyproject.toml, Cargo.toml, Makefile, scripts/build.sh, scripts/build*.sh, **/version/**, **/build/**
alwaysApply: false
---

# CLI 项目版本号与构建契约规则

> 只约束版本号如何管理，以及构建流程如何消费版本号。
> 不规定必须使用 GitHub Actions、GitLab CI、Jenkins 或任何特定发布平台。

---

## 一、版本号管理

### 1.1 单一版本来源 (Single Source of Truth)

项目必须有且仅有一个权威版本定义位置，所有引用（构建注入、CI、文档）都从此处读取，禁止把同一个产品版本硬编码到多个文件。

**推荐文件**：`version.json`

```json
{
  "version": "v1.2.3"
}
```

**多语言版本源对照**：

| 语言/生态 | 版本源文件 | 备注 |
|-----------|-----------|------|
| Go | `version.json` | Go 模块自身不携带产品版本 |
| Node.js | `package.json` 的 `version` 字段 | npm 生态原生支持 |
| Python | `pyproject.toml` 的 `[project] version` | 或构建时生成 `_version.py` |
| Rust | `Cargo.toml` 的 `version` | cargo 生态原生支持 |
| C/C++ | `version.json` 或 `VERSION` 文件 | 需构建时注入 |

### 1.2 语义化版本 (SemVer)

格式：`vMAJOR.MINOR.PATCH[-PRERELEASE]`

- **PATCH**：向后兼容的修复
- **MINOR**：向后兼容的新功能
- **MAJOR**：破坏性变更

示例：`v1.0.0`、`v2.1.0-beta.1`、`v1.2.3-rc.2`

### 1.3 构建时注入版本

构建产物应在编译或打包时注入版本号，而不是让运行时逻辑依赖仓库中的版本文件。

**各语言示例**：

| 语言 | 方式 |
|------|------|
| Go | `-ldflags "-X main.Version=$VERSION -X main.BuildTime=$BUILD_TIME"` |
| Rust | `env!("CARGO_PKG_VERSION")` 或 `build.rs` |
| Python | `importlib.metadata.version()` 或构建时写入 `_version.py` |
| Node.js | `process.env` 或打包阶段注入 |
| C/C++ | `-DVERSION=\"$VERSION\"` 预处理器宏 |

**版本提取脚本**：

```bash
VERSION=$(jq -r '.version' version.json)
BUILD_TIME=$(date -u '+%Y-%m-%dT%H:%M:%SZ')
COMMIT=$(git rev-parse --short HEAD 2>/dev/null || echo "unknown")
```

### 1.4 版本回退策略

代码中应设默认值，按以下优先级获取版本：

1. **构建时注入的版本**
2. **本地版本源文件**（开发环境）
3. **硬编码 `dev`**（兜底）

### 1.5 版本比较

- 去掉 `v` 前缀，按 `major.minor.patch` 三段数值比较
- `dev`、`unknown`、空值应始终视为需要更新
- 预发布版本顺序：`alpha < beta < rc < 正式版`

---

## 二、版本号与构建的关系

### 2.1 构建输入契约

构建脚本或构建系统必须能从单一版本源读取版本，或显式接收外部注入的版本号。

推荐接受以下环境变量：

```bash
VERSION          # 版本号；默认从版本源读取
BUILD_TIME       # 构建时间；默认当前时间
COMMIT           # git commit hash；默认自动获取
BUILD_PLATFORMS  # 目标平台；默认由构建系统决定
OUTPUT_DIR       # 输出目录；默认由构建系统决定
```

### 2.2 构建输出契约

- 可执行产物必须能通过 `--version` 或等价命令反映出构建时注入的版本
- 版本相关元数据应与当前源码提交一致，避免出现“二进制版本号”和“提交内容”脱钩
- 如果项目有更新检查或发布清单，版本字段也必须来自同一版本源

### 2.3 边界说明

本规则只定义版本与构建之间的数据契约，不定义具体的编排方式。

这意味着以下内容都可以由项目自行选择：

- 用 GitHub Actions、GitLab CI、Jenkins，还是本地脚本完成构建
- 是推送版本变更后自动发布，还是人工触发构建
- 是否由 CI 自动打 tag、上传产物、生成校验和或发布说明

唯一要求是：无论采用哪一种流程，后续构建与发布步骤都必须消费同一个权威版本源。
