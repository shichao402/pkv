<!-- 本文件由 `dec pull` 从 .dec/cache/vikunja/ 渲染生成，请勿直接编辑。
     修改流程：编辑 .dec/cache/vikunja/... → dec push → dec pull 验证 -->

# Vikunja MCP 集成规则

当项目加载 vikunja-mcp 资产时，遵循以下规则确保顺利集成。

## ⚠️ 重要约束

**所有 Dec 资产的安装、更新、推送都必须通过 `dec` CLI 完成，禁止直接操作底层 git 仓库。**

常用命令：

- 拉取当前项目已启用资产：`dec pull`
- 推送 `.dec/cache/` 中的资产变更：`dec push`
- 搜索远程资产：`dec search "<query>"`
- 查看当前项目资产：`dec list`

如果需要新增通用资产，先修改 `.dec/cache/` 与 `.dec/config.yaml`，再执行 `dec push`。
如果 `dec` 命令执行失败，应把错误反馈给用户，由用户决定是否继续，而不是绕过 `dec` 直接操作 git。

## 🧩 变量化原则

Dec 中保存的共享 Vikunja 资产应只把真正会按项目变化的值做成变量，其余稳定流程约定直接固化在共享资产里。

- 可复用资产里保留少量必要变量，例如默认项目名、任务文档目录
- 项目本地的具体取值写入 `.dec/vars.yaml`
- `dec pull` 时再把这些变量替换进当前项目安装到 IDE 的实际配置
- 不要把稳定的流程 bucket 名、type label 名、默认视图名也全部变量化
- 当用户确认了项目本地默认值后，应该优先整理进 vars，而不是把项目特有事实回写进通用资产

这条规则的目的，是让 Dec 远端永远保存可抽象、可迁移的版本，而不是保存某个项目的一次性定制文案。

## 🔒 目标项目确认（强制）

Agent 在执行任何 Vikunja 写操作前，**必须先确认本轮操作的目标 Vikunja 项目**。禁止跳过此步骤。

这里的“确认”是指拿到一个唯一、可复核的项目名称或项目 ID，而不是凭仓库名、上下文印象或方便程度去猜。

### 允许的目标来源

按优先级从高到低，允许使用以下来源确认目标项目：

1. 用户明确提供的项目名称或项目 ID
2. 当前任务、issue、评论、链接中已经明确指向的项目
3. 当前仓库文档里明确声明的默认 Vikunja 项目
4. 项目级变量，例如 `VIKUNJA_PROJECT`，但它只应被视为默认值，不是无条件真相

### 明确禁止的推断方式

以下方式都不能单独作为目标确认依据：

- 直接把当前仓库目录名当成 Vikunja 项目名
- 因为“当前就在这个仓库里”就默认该仓库绑定唯一项目
- 因为某个项目最近操作过，就继续对它写入
- 在多个相似项目之间自行挑一个最像的

一个仓库可能合法地对应多个 Vikunja 项目，例如主 backlog、通用 intake、研究池或跨团队项目。
这不是例外，而是正常情况。

### 项目确认流程

1. 先收集显式目标来源。
2. 调用 `vikunja_list_projects` 查询候选项目。
3. 用明确的项目名或项目 ID 解析唯一目标。
4. 如果存在多个相似候选，必须停下来让用户确认。
5. **只有在目标唯一确认后**，才允许继续执行 create、update、comment、move、complete、delete 等写操作。

### 强制约束

- **创建任务时，必须使用已确认项目的 `project_id`。**
- **查询任务时，优先使用项目内接口，或显式使用 `project_id` 过滤。** 禁止把全局查询结果直接当作当前目标项目的 backlog。
- **禁止对未确认项目执行任何写操作**，包括 project、task、comment、bucket、share、webhook 等修改。
- **如果本轮需要从一个项目切换到另一个项目，必须重新确认目标。**

### 必须中断的情况

以下任何一种情况发生时，**必须立即停止操作并等待用户明确指示**，禁止自行决定：

- 没有足够信息确认本轮目标项目
- 找到多个名称相近或语义相近的候选项目
- 用户请求同时涉及多个项目，但没有明确指出当前这一轮要操作哪个
- 当前仓库文档给出的默认项目，与用户本轮明确指定的项目发生冲突且无法自动化解
- 任何可能导致任务、评论或状态被写入错误项目的模糊指令

**中断时的回复模板：**

> 目前还不能唯一确认这轮操作的 Vikunja 项目。请明确：
> 1. 这轮要操作哪个项目？（请提供项目名称或 ID）
> 2. 如果有多个项目都相关，请先告诉我当前这一轮要以哪个项目为主。

## 🧱 通用项目结构建议

如果要把这套流程沉淀为可复用的 Vikunja 结构，默认应按“每个交付项目各自管理自己的 issue intake”来设计，而不是把多个项目的问题混进一个总 Inbox。

### 结构原则

- bucket 表示流程阶段，不表示工作类型
- label 表示事项类型，不表示流程阶段
- priority 字段表示紧急度，不再复制一套优先级标签
- saved filter 只是可选观察视图，不是事实来源；agent 推进优先直接调用 MCP 查询
- 如果团队确实需要跨项目 intake，应该单独建立明确命名的 intake 项目，而不是把普通项目 filter 混成全局收件箱

### 推荐 bucket

- `待分诊`：刚进入项目，还没有完成归一化
- `待补充`：问题存在，但证据、复现、范围或责任信息还不够
- `待研判`：问题已经足够明确，下一步是研究、设计权衡或决策
- `待排期`：方向已接受，等待进入具体执行
- `执行中`：本轮正在推进
- `阻塞`：已知依赖、外部条件或决策阻塞

任务完成优先使用 Vikunja 的 `done` 字段表达。
除非团队明确需要 `已完成` 这类完成列，否则不建议再额外复制一个完成 bucket。

### 推荐 label

- `type:bug`
- `type:feature`
- `type:improvement`
- `type:research`
- `type:decision`
- `type:follow-up`

可按团队需要补充 `type:chore` 等少量类型标签，但不要把 source、priority、stage 再复制成大批标签。
来源标签不是默认必需项。用户反馈、开发者自提、讨论中发现的问题，优先写进描述、评论或 reporter 上下文；只有当团队已经有稳定判定规则时，才再引入 `source:*` 体系。

### 推荐 saved filter

agent 推进不依赖 saved filter。只有在用户明确希望保留人类可点击的快捷视图时，才创建 saved filter。
创建 saved filter 时，过滤条件里仍然应显式限制目标项目，而不是默认跨所有项目。

建议至少准备这些过滤视图：

- `Intake`：当前项目中位于 `待分诊`、`待补充`、`待研判` 且未完成的任务
- `Needs Decision`：当前项目中带 `type:decision` 且未完成的任务
- `Needs Research`：当前项目中带 `type:research` 且未完成的任务
- `Ready / Active`：当前项目中位于 `待排期` 或 `执行中` 且未完成的任务
- `Blocked`：当前项目中位于 `阻塞` 且未完成的任务

### 最小视图集

每个项目通常至少保留：

- 一个列表视图，用于排序、批量检查和搜索
- 一个看板视图，用于 bucket 驱动的推进
- 需要时再补充归档或只读视图，不建议先堆很多视图

如需把上述结构真正落地到某个项目，而不是只讨论原则，使用 `$vikunja-project-bootstrap` 单独执行初始化或整顿。

## 📌 环境配置

### 必需的环境变量

当前 Dec 资产为了兼容既有项目，继续透传 `VIKUNJA_URL`。
`@shichao402/vikunja-mcp` 同时支持 `VIKUNJA_BASE_URL` 和 `VIKUNJA_URL`，推荐使用实例根地址，而不是手动拼接 `/api/v1`。

在项目的 `.env` 文件或 `config.yaml` 中至少配置以下变量：

#### 方式 1: .env 文件（推荐本地开发）

```bash
VIKUNJA_URL=https://vikunja.example.com
VIKUNJA_API_TOKEN=your-api-token-here
```

#### 方式 2: config.yaml（推荐生产/容器化）

```yaml
secrets:
  VIKUNJA_URL: "https://vikunja.example.com"
  VIKUNJA_API_TOKEN: "your-token-here"
```

如需和上游包文档保持一致，也可以改用 `VIKUNJA_BASE_URL`；当前包会把 `VIKUNJA_URL` 作为等价别名处理。

### URL 格式规范

- ✅ 推荐: `https://vikunja.example.com`
- ✅ 兼容: `https://vikunja.example.com/api/v1`（包会自动规范化）
- ❌ 错误: `vikunja.example.com`（缺少协议）
- ❌ 错误: `/api/v1`（不是完整 URL）

## 🔧 配置集成

### 集成方式选择

#### 选项 A: .mcp.json（单项目 / 单 Agent）

适用场景：项目中只需要一个 MCP 服务器配置

在项目根目录创建或编辑 `.mcp.json`：

```json
{
  "mcpServers": {
    "vikunja": {
      "command": "mise",
      "args": ["exec", "node@20", "--", "npx", "-y", "@shichao402/vikunja-mcp"],
      "env": {
        "VIKUNJA_URL": "${VIKUNJA_URL}",
        "VIKUNJA_API_TOKEN": "${VIKUNJA_API_TOKEN}"
      }
    }
  }
}
```

然后在 IDE 配置中加载此文件（CodeBuddy Code 会自动识别）。

#### 选项 B: agents.yaml（多 Agent 场景）

适用场景：项目中多个 Agent 需要使用 Vikunja，或需要灵活配置不同 Agent 的权限

在 `agents.yaml` 中为需要的 Agent 添加 Vikunja 插件：

```yaml
agents:
  planning-agent:
    plugins:
      vikunja:
        command: mise
        args: ["exec", "node@20", "--", "npx", "-y", "@shichao402/vikunja-mcp"]
        env:
          VIKUNJA_URL: ${VIKUNJA_URL}
          VIKUNJA_API_TOKEN: ${VIKUNJA_API_TOKEN}

  execution-agent:
    plugins:
      vikunja:
        command: mise
        args: ["exec", "node@20", "--", "npx", "-y", "@shichao402/vikunja-mcp"]
        env:
          VIKUNJA_URL: ${VIKUNJA_URL}
          VIKUNJA_API_TOKEN: ${VIKUNJA_API_TOKEN}
```

## ✅ 启动检查清单

加载 Vikunja MCP 后，按以下步骤验证：

### 1. 环境变量验证

```bash
echo "VIKUNJA_URL=${VIKUNJA_URL:-$VIKUNJA_BASE_URL}"
echo "VIKUNJA_API_TOKEN=${VIKUNJA_API_TOKEN:0:10}***"  # 只输出前 10 个字符用于验证
```

**预期结果**:
- URL 环境变量显示完整实例地址
- VIKUNJA_API_TOKEN 显示 token 的前 10 个字符

### 2. 网络连通性测试

```bash
curl -s "${VIKUNJA_URL:-$VIKUNJA_BASE_URL}/api/v1/info" | jq .
```

**预期结果**：返回 JSON 格式的 Vikunja 服务信息

### 3. MCP 服务启动验证

先执行一次本地 smoke test：

```bash
mise exec node@20 -- npx -y @shichao402/vikunja-mcp --help
```

如果需要继续验证 IDE 集成，再启动 IDE 并观察控制台日志：

- ✅ 成功：`[MCP] vikunja server started`
- ❌ 失败：查看错误日志，常见错误见"故障排除"章节

### 4. MCP 工具可用性验证

在 Agent 中尝试使用 Vikunja 工具（如果 IDE 支持）：
- 列出项目：`vikunja_list_projects`
- 查询任务：`vikunja_list_tasks`，并通过 `filter` 限定 `project_id`
- 创建任务：`vikunja_create_task`

## 🔄 常见操作规范

### 创建任务

通过 MCP 工具 `vikunja_create_task` 创建任务时，直接传工具输入：

```json
{
  "project_id": 1,
  "title": "Task Title",
  "description": "Task description with context",
  "priority": 3,
  "due_date": "2026-04-30T12:00:00+08:00"
}
```

**字段说明**:
- `project_id`: 必需，通过 `vikunja_list_projects` 获取
- `title`: 必需，任务标题
- `description`: 可选，支持 Markdown 格式
- `priority`: 可选，整数优先级
- `due_date`: 可选，ISO 8601 时间戳

如需给任务打标签，先使用 `vikunja_list_labels` / `vikunja_create_label` 获取或创建标签，再调用 `vikunja_add_label_to_task`。

### 查询项目内任务

`vikunja_list_tasks` 默认会跨当前 token 可见的所有项目检索。
如果只查询当前确认的目标项目，必须显式带 `filter`，例如：

```json
{
  "filter": "project_id = 123",
  "sort_by": ["priority", "due_date"],
  "order_by": "desc"
}
```

### 更新任务状态

通过 `vikunja_update_task` 更新任务时，记得带上 `task_id`：

```json
{
  "task_id": 123,
  "done": true,
  "description": "更新的描述信息"
}
```

**常见字段**:
- `task_id`: 必需，目标任务 ID
- `done`: 标记任务完成/未完成
- `priority`: 修改优先级
- `description`: 更新描述

### 移动看板卡片

已验证的行为：

- `PUT /filters` 创建 saved filter 可以正常使用
- `POST /tasks/bulk` 虽然可以更新任务字段，但不应作为真实 kanban bucket 迁移手段
- 真正的看板卡片移动应使用专门的 bucket 任务移动接口：`POST /projects/{project}/views/{view}/buckets/{bucket}/tasks`

如果当前 MCP 暴露了原始 REST 工具，优先调用对应的 task-bucket move endpoint，不要单纯写 `bucket_id` 后假设看板位置已经同步。

### 任务关联和链接

`@shichao402/vikunja-mcp` 的任务关系能力主要通过原始 REST 工具暴露，例如 `vikunja_api_put_tasks_task_id_relations`。

使用这类工具时，参数以 MCP tool schema 为准；通常需要提供当前任务 ID、关系类型以及关联任务 ID。

## 🐛 故障排除

### 错误 1: "无法连接到 Vikunja 服务"

**原因**: URL 或网络不可达

**检查步骤**:
1. 验证 `VIKUNJA_URL` 或 `VIKUNJA_BASE_URL` 是否正确
2. 确认它是带 `http://` 或 `https://` 的完整 URL
3. 测试网络：`ping vikunja.example.com`
4. 测试 API：`curl -v "${VIKUNJA_URL:-$VIKUNJA_BASE_URL}/api/v1/info"`

### 错误 2: "认证失败 (401 Unauthorized)"

**原因**: Token 无效或已过期

**解决方案**:
1. 登录 Vikunja 界面 → 用户设置 → API Token
2. 删除旧 Token，生成新 Token
3. 更新 `.env` 或 `config.yaml` 中的 `VIKUNJA_API_TOKEN`
