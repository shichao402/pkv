<!-- 本文件由 `dec pull` 从 .dec/cache/vikunja/ 渲染生成，请勿直接编辑。
     修改流程：编辑 .dec/cache/vikunja/... → dec push → dec pull 验证 -->

# Vikunja MCP 集成规则

当项目加载 Vikunja 相关共享资产时，遵循以下规则。

## ⚠️ Dec 约束

所有远端资产的安装、更新、推送都必须通过 `dec` CLI 完成。不要直接操作 Dec 远端仓库。

常用命令：

- `dec pull`：拉取当前项目已启用资产
- `dec push`：推送 `.dec/cache/` 中的资产变更
- `dec search "<query>"`：搜索远端资产
- `dec list`：查看当前项目资产

如果 `dec` 不可用或执行失败，应把情况反馈给用户，而不是绕过 `dec` 修改远端资产。

## 🧩 变量化原则

共享 Vikunja 资产只保留真正会随项目变化的值为变量，其余流程约定直接固化。

- 允许变量化的通常只有默认项目名、可选任务文档目录这类项目本地值
- 项目本地取值写入 `.dec/vars.yaml`
- `dec pull` 时再把这些变量渲染进 IDE 安装副本
- 不要把稳定 bucket、稳定 type label、默认视图名做成变量
- 当用户确认了项目本地默认值，应优先整理进 vars，而不是回写进共享资产文案

共享资产保存的是抽象后的方法，不是某个仓库的一次性配置。

## 🗂️ 仓库本地文档边界

共享 Vikunja 资产应该能独立成立。仓库本地文档只保留真正项目相关的信息。

适合保留在仓库里的通常只有：

- 默认目标项目名
- 可选的任务文档目录
- commit traceability 格式
- 哪些改动必须同步更新架构文档
- 项目自己的 parked / skip 规则

不要在每个仓库里再复制一份完整生命周期、通用 bucket 含义、通用 issue 流程。

## 🔒 目标项目确认

Agent 在执行任何 Vikunja 写操作前，必须先确认本轮操作的目标项目。

这里的“确认”是指拿到唯一、可复核的项目名称或项目 ID，而不是凭上下文猜测。

### 允许的目标来源

按优先级从高到低，允许使用以下来源确认目标项目：

1. 用户明确提供的项目名称或项目 ID
2. 当前任务、评论、链接里已经明确指向的项目
3. 仓库文档里声明的默认 Vikunja 项目
4. 项目级变量，例如 `VIKUNJA_PROJECT`，但它只是默认值

### 明确禁止的推断方式

以下方式都不能单独作为目标确认依据：

- 直接把当前仓库目录名当成 Vikunja 项目名
- 因为“当前就在这个仓库里”就默认只对应一个项目
- 因为某个项目最近操作过，就继续对它写入
- 在多个相似项目之间自行挑一个最像的

### 项目确认流程

1. 先收集显式目标来源。
2. 调用 `vikunja_list_projects` 查询候选项目。
3. 用明确的项目名或项目 ID 解析唯一目标。
4. 如果存在多个相似候选，停下来让用户确认。
5. 只有在目标唯一确认后，才允许 `create`、`update`、`comment`、`move`、`complete`、`delete` 等写操作。

### 强制约束

- 创建任务时，必须使用已确认项目的 `project_id`
- 查询任务时，优先使用项目内接口，或显式使用 `project_id` 过滤
- 禁止对未确认项目执行任何写操作
- 如果本轮要切换项目，必须重新确认目标

### 必须中断的情况

以下任一情况出现时，必须立即停止并等待用户明确指示：

- 没有足够信息确认本轮目标项目
- 找到多个名称相近或语义相近的候选项目
- 用户请求同时涉及多个项目，但没有明确指出当前这一轮要操作哪个
- 仓库默认项目与用户本轮明确指定的项目冲突且无法自动化解
- 任何可能导致任务、评论或状态被写入错误项目的模糊指令

中断时建议直接说明：目前还不能唯一确认这轮操作的 Vikunja 项目，请用户提供项目名称或 ID。

## 🧱 Canonical 项目结构

共享资产只描述一套 canonical 结构。
如果某个项目仍然保留旧 bucket、旧标签或旧完成约定，应整顿项目本身，而不是把旧结构继续写进共享规则。

### 结构原则

- bucket 表示流程阶段，不表示工作类型
- label 表示事项类型，不表示流程阶段
- priority 字段表示紧急度，不再复制一套优先级标签
- 如果团队需要跨项目 intake，应单独建立 intake 项目，而不是把普通项目混成一个总 inbox

### Active Buckets

- `待分诊`
- `待补充`
- `待研判`
- `待排期`
- `执行中`
- `阻塞`

完成语义使用 Vikunja 的 `done` 字段。
如果项目需要可视化完成列，应该把该列配置成看板的 done bucket，而不是额外再发明完成标签。
明确关闭但不交付的事项可以放入项目自己的终态 bucket，例如 `已关闭`，但这不是共享选择逻辑的一部分。

### Type Labels

- `type:bug`
- `type:feature`
- `type:improvement`
- `type:research`
- `type:decision`
- `type:follow-up`

可按团队需要补充少量标签，例如 `type:chore`，但不要再复制 source、priority、stage 这类体系。
新条目和新项目统一使用 `type:*` 标签，不要继续给新工作加 plain `bug`、`feature`、`improvement` 这类旧标签。

### 最小视图集

每个项目至少保留：

- 一个列表视图，用于排序、批量检查和搜索
- 一个看板视图，用于 bucket 驱动的推进

如需把项目真正初始化或整顿到这套结构，使用 `$vikunja-project-bootstrap`。

## 📝 任务内容格式

所有 Vikunja 任务写入的文本内容必须使用 Markdown 格式。

适用范围：

- 任务 `description`（包括新建、更新时写入的内容）
- 任务 `comment`（包括自治动作日志、交付回写、讨论记录）
- 项目 `description`

基本要求：

- 使用标准 Markdown 语法：`##` 标题、`-` 列表、`**粗体**`、\`行内代码\`、\`\`\` 代码块、`> 引用`、`[文本](链接)`
- 结构化段落比散文更好读：背景、目标、范围、验收、参考等信息用小标题分节
- 代码、路径、命令、配置键名一律使用行内代码或代码块，避免和正文混在一起
- 不要在正文里混用 HTML 标签，除非 Markdown 语法确实无法表达

存储与 API 的真实行为（经 API 实测）：

- Vikunja 服务端**按原文存储** description 和 comment，API 写入什么就存什么，API 读出来就是原样
- 通过 MCP / REST API 写入的 Markdown **无需额外包裹**（不要用 fenced code block 包裹整段文本，会把全文渲染为代码块，可读性反而下降）
- **WebUI 编辑会触发 HTML 化**：如果用户在 Vikunja Web 端打开一张 Markdown 任务并触发编辑保存，WYSIWYG 编辑器会把原文经 Tiptap / ProseMirror 转换后写回为 `<p>...</p>` 形式，丢掉换行与结构。这是已知的用户行为层陷阱，不是存储层问题
- agent 读到 HTML 化的 description 时应容忍，并可在必要时通过 API 重写回 Markdown；但重写后若用户再次从 WebUI 编辑，会再次被 HTML 化

### `vikunja_update_task` 的更新语义

调用任务更新接口时：

- **默认 PATCH 合并**（vikunja-mcp `0.3.0+`）：只传你要改的字段即可，MCP 层会先 GET 当前任务 → 过滤 read-only 字段 → 把你的字段合并在上面 → POST 合并后的完整 body。未传字段会被保留。
- **显式全量替换**：传 `_replace: true` 可以跳过合并，直接 POST 你显式传入的字段。Vikunja 会把未传字段重置为类型默认值（description 清空、priority 归 0、title 重置等）。只在真的要"重置未传字段"时使用，其他场景都不要传这个开关。
- **read-only 字段**会在合并时被自动剥离：`created` / `updated` / `done_at` / `created_by` / `identifier` / `index` / `related_tasks` / `reactions` / `attachments` / `cover_image_attachment_id` / `labels` / `assignees` / `subscription` / `buckets` / `reminders`。如果要改这些，请走它们各自的专用接口（label / assignee / relation / comment 等）。
- 这条默认行为对所有涉及任务字段修改的自治动作都生效（auto-reprioritize、auto-append-description 等），不再需要调用方手工做读-合并-写。

如果项目仍在使用 `@shichao402/vikunja-mcp@0.2.1` 或更早版本，`vikunja_update_task` 仍是 PUT 全量替换语义，这种情况下需要先 `vikunja_api_get_tasks_id` 读出完整任务、合并本次要改的字段、完整传入所有需要保留的字段再调 update。把这些项目升到 `0.3.0+` 之后就不再需要这一 workaround。

为什么要固化这条规则：

- Vikunja Web 端、移动端、以及后续可能接入的可视化插件默认都按 Markdown 渲染 description 和 comment
- 统一格式后，任务内容可以直接被检索、摘要、跨工具复用，不需要二次清洗
- 人和 agent 的协作界面一致，避免"谁写的谁格式"的混乱
- 明确 update 语义（0.3.0+ 默认 PATCH 合并，`_replace: true` 做全量替换）可以避免 agent 因为不了解 API 契约导致数据丢失

## 📌 共享 MCP 环境变量

共享 Vikunja MCP 资产统一使用以下环境变量：

```bash
VIKUNJA_URL=https://vikunja.example.com
VIKUNJA_API_TOKEN=your-api-token-here
```

在 `config.yaml` 中也可以这样提供：

```yaml
secrets:
  VIKUNJA_URL: "https://vikunja.example.com"
  VIKUNJA_API_TOKEN: "your-token-here"
```

### URL 规范

- `VIKUNJA_URL` 使用实例根地址
- 必须包含 `http://` 或 `https://`
- 不要把 `/api/v1` 当成共享 MCP 配置标准写法
- 如果某个应用自己的 REST 代理配置需要 `/api/v1`，把它保留在应用本地运行时配置里，不要和共享 MCP 约定混用

## 🔧 配置集成

### 选项 A：`.mcp.json`

```json
{
  "mcpServers": {
    "vikunja": {
      "command": "mise",
      "args": ["exec", "node@20", "--", "npx", "-y", "@shichao402/vikunja-mcp"],
      "env": {
        "VIKUNJA_URL": "${VIKUNJA_URL}",
        "VIKUNJA_API_TOKEN": "${VIKUNJA_API_TOKEN}"
      }
    }
  }
}
```

### 选项 B：`agents.yaml`

```yaml
agents:
  planning-agent:
    plugins:
      vikunja:
        command: mise
        args: ["exec", "node@20", "--", "npx", "-y", "@shichao402/vikunja-mcp"]
        env:
          VIKUNJA_URL: ${VIKUNJA_URL}
          VIKUNJA_API_TOKEN: ${VIKUNJA_API_TOKEN}
```

## ✅ 启动检查清单

1. 验证环境变量：

```bash
echo "VIKUNJA_URL=${VIKUNJA_URL}"
echo "VIKUNJA_API_TOKEN=${VIKUNJA_API_TOKEN:0:10}***"
```

2. 验证实例可达：

```bash
curl -s "${VIKUNJA_URL}/api/v1/info" | jq .
```

3. 验证 MCP 包可启动：

```bash
mise exec node@20 -- npx -y @shichao402/vikunja-mcp --help
```

4. 在 Agent 中验证工具：

- 列出项目：`vikunja_list_projects`
- 查询任务：`vikunja_list_tasks`
- 创建任务：`vikunja_create_task`

## 🔄 常见操作规范

### 创建任务

```json
{
  "project_id": 1,
  "title": "Task Title",
  "description": "Task description with context",
  "priority": 3,
  "due_date": "2026-04-30T12:00:00+08:00"
}
```

如需标签，先通过 `vikunja_list_labels` / `vikunja_create_label` 获取或创建，再调用 `vikunja_add_label_to_task`。

### 查询项目内任务

`vikunja_list_tasks` 默认会跨 token 可见项目检索。
如果只查询当前目标项目，必须显式带 `filter`：

```json
{
  "filter": "project_id = 123",
  "sort_by": ["priority", "due_date"],
  "order_by": "desc"
}
```

### 更新任务

```json
{
  "task_id": 123,
  "done": true,
  "description": "更新的描述信息"
}
```

### 移动看板卡片

真实的看板卡片移动应使用 bucket 任务移动接口：

- `POST /projects/{project}/views/{view}/buckets/{bucket}/tasks`

不要单纯写 `bucket_id` 后就假设看板位置已经同步。

### 任务关系

任务关系通常通过原始 REST 工具暴露，例如 `vikunja_api_put_tasks_task_id_relations`。
使用时以工具 schema 为准，通常需要当前任务 ID、关系类型、关联任务 ID。

## 🐛 故障排除

### 无法连接到 Vikunja 服务

检查顺序：

1. `VIKUNJA_URL` 是否为完整 URL
2. 实例是否可达：`curl -v "${VIKUNJA_URL}/api/v1/info"`
3. 网络或 DNS 是否正常

### 认证失败

检查 `VIKUNJA_API_TOKEN` 是否有效，必要时重新生成。

### MCP 服务启动失败

检查顺序：

1. `mise` 是否可用
2. `node@20` 是否可执行
3. `mise exec node@20 -- npx -y @shichao402/vikunja-mcp --help` 是否成功

### 找不到项目或任务

先确认目标项目是否唯一，再用 `vikunja_list_projects` / `vikunja_list_tasks` 复查，不要把全局结果直接当作当前项目的 backlog。
