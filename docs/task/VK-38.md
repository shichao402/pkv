# VK-38 `get <folder> note` 初始化遇到已存在文件时直接失败，缺少冲突处理策略

- Vikunja: `PKV#38`
- Status: `执行中`

## 本轮停止条件

- `pkv get <folder> note` 在真正写文件前完成冲突预检
- 遇到多个冲突时一次性返回冲突清单，而不是命中第一个就退出
- 预检失败时不产生部分删除、部分重命名或部分写入
- 有自动化测试覆盖新的冲突路径

## 实施步骤

1. 梳理 `internal/note/syncer.go` 当前同步流程，识别会在失败前产生副作用的步骤
2. 重构为预检 + 应用两阶段，同步前先收集所有问题并聚合返回
3. 为未追踪同名文件、已追踪本地改动、重命名目标冲突等场景补测试
4. 更新 README 中 note 冲突处理说明，确保 CLI 行为和文档一致
5. 执行相关测试并记录结果

## 测试口径

- `go test ./internal/note`
- 如有必要，补跑受影响命令层测试

## 结果记录

- 已将 note 同步重构为“预检 + 应用”两阶段，冲突时先聚合报错，再保证本地零副作用退出
- 已覆盖未追踪同名文件、多个冲突聚合、远端路径别名冲突、文件/目录形态冲突、已追踪文件改名进子目录等场景
- 已更新 README，说明新的冲突提示和无副作用语义

## 验证记录

- `go test ./internal/note ./...`

## 涉及文件和模块

- `internal/note/syncer.go`
- `internal/note/syncer_test.go`
- `README.md`
