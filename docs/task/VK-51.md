# VK-51 `bw` CLI 版本探测与兼容性异常需要在启动前置暴露

- Vikunja: `PKV#51`
- Project: `PKV` (ID `8`)
- Status: `已完成`

## 本轮停止条件

- 在进入 `EnsureUnlocked` 主流程前完成 `bw` 可执行文件与版本探测
- 当 `bw --version` 失败、返回空值或输出格式异常时，返回明确的兼容性错误，而不是等到后续命令失败
- 有自动化测试覆盖安装缺失、版本探测失败、异常输出等路径
- README 补充相关排查说明

## 实施步骤

1. 梳理 `internal/bw` 当前安装检查和客户端入口，确定版本探测最小接入点
2. 增加 `bw --version` 探测与输出解析，优先保证“早失败、错误清晰”
3. 为版本探测成功、失败、空输出、异常格式补充测试
4. 更新 README 的依赖/故障排查说明，补充版本兼容性排查路径
5. 执行相关测试并记录结果

## 测试口径

- `go test ./internal/bw`
- 如入口行为受影响，补跑 `go test ./...`

## 结果记录

- 已将 `bw` 可执行文件检查扩展为“存在性 + 版本探测”两步，进入 `EnsureUnlocked` 前先执行 `bw --version`
- 当 `bw --version` 失败、输出为空或格式异常时，现在会直接返回带路径和版本探测上下文的错误
- 保留原有缺失安装提示，同时新增针对异常版本输出的自动化覆盖与 README 排查指引

## 验证记录

- `go test ./internal/bw`
- `go test ./...`

## 涉及文件和模块

- `internal/bw/check.go`
- `internal/bw/client.go`
- `internal/bw/*_test.go`
- `README.md`
