# VK-39 `note` 可针对 mise 等支持 `conf.d` 的工具提供碎片化落盘方案，减少原子文件冲突

- Vikunja: `PKV#39`
- Status: `已关闭`

## 本轮停止条件

- 明确是否保留 `mise conf.d` / note 目标元数据方案，而不是继续让主线实现与文档处于摇摆状态
- 当前主线回到“note 名称即目标相对路径”的单一模型，README、测试和实现保持一致
- 在任务记录中留下结论、验证结果和后续独立方向，避免把已否决方案继续当作执行中事项

## 实施步骤

1. 梳理当前 note 落盘逻辑，抽出“远端 note -> 本地目标路径”的解析层
2. 为 Secure Note 增加 note 目标元数据约定，支持默认单文件、`mise conf.d` 策略和显式路径覆盖
3. 在同步流程中接入新的目标解析，保证重命名、冲突预检和状态追踪继续成立
4. 为目标解析和 `mise conf.d` 同步场景补测试
5. 更新 README，说明元数据字段、CLI 用法和适用边界

## 测试口径

- `go test ./internal/note ./internal/bw/... ./cmd/...`

## 结果记录

- 本轮最终没有保留 `mise_conf_d` / note 策略元数据方案，已按决策回退到仅依赖 note 名称路径同步的模型
- 当前 `pkv get <folder> note` 仍保持“note 名称即目标相对路径”的语义，继续复用既有预检、冲突保护和状态追踪
- README 和测试已同步回退，避免对外暴露暂不采纳的策略字段
- 后续如果要支持目录级选择或目录树同步，应作为新的设计任务独立推进，而不是继续叠加 note 元数据分支逻辑

## 验证记录

- `go test ./internal/note ./internal/bw/... ./cmd/...`
- `go test ./...`

## 提交记录

- `7bff296` `feat(note): support fragmented mise conf.d targets [VK-39]`
- `391f802` `revert(note): drop fragmented conf.d strategy [VK-39]`

## 涉及文件和模块

- `internal/bw/types/types.go`
- `internal/note/*.go`
- `cmd/resources.go`
- `internal/securenote/securenote.go`
- `README.md`
